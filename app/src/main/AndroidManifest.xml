<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/icon_book_shop"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_book_shop"
        android:supportsRtl="true"
        android:theme="@style/Theme.Books_v1_1"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.log.LoginActivity" />
        <activity android:name="com.example.log.RegisterActivity" />
        <activity android:name=".HomeActivity"/>
        <activity android:name=".ChatActivity"/>
        <activity android:name=".TrashActivity"/>
        <activity android:name=".VasuaPageActivity"/>
    </application>

</manifest>